FROM mcr.microsoft.com/playwright/python:v1.42.0-jammy

WORKDIR /app
COPY . .

RUN pip install --no-cache-dir -r requirements.txt

# ⛳ CRUCIAL: Force Playwright to re-install Chromium + dependencies
RUN playwright install --with-deps

CMD ["python", "main.py"]

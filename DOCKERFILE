FROM python:3.12-slim                    # ❌ Don't use slim Python base
RUN apt-get update && apt-get install…  # ❌ Unneeded: Playwright image includes all this
RUN playwright install --with-deps      # ❌ Already done in the base image
RUN pip install -r requirements.txt     # ✅ Only needed once, with --no-cache-dir
